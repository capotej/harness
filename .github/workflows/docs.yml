name: Deploy Docs
on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - "zensical.toml"
      - ".github/workflows/docs.yml"
      - "README.md"
      - "AGENTS.md"
  pull_request:
    paths:
      - "docs/**"
      - "zensical.toml"
      - ".github/workflows/docs.yml"
      - "README.md"
      - "AGENTS.md"
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write

# A docs change on a PR only needs to prove the site builds; deploying is gated
# to non-PR events (push to main, or workflow_dispatch), preserving the original
# deploy behavior.
jobs:
  build:
    name: Build docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
      - run: uv tool install zensical==0.0.43
      - name: Build site
        run: zensical build --clean
      # Only package the site for an actual deploy; skip on PR builds.
      - name: Configure Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6
      - name: Upload artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: site

  deploy:
    name: Deploy to GitHub Pages
    if: github.event_name != 'pull_request'
    needs: build
    concurrency:
      group: pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    env:
      # Job-level so the webhook step's `if` can test it (the secrets context
      # is not available in step-level conditionals).
      AMPLIFY_DOCS_WEBHOOK_URL: ${{ secrets.AMPLIFY_DOCS_WEBHOOK_URL }}
    steps:
      - uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5
        id: deployment
      # The marketing site (harness.boldblack.ai, AWS Amplify) mirrors the
      # published Pages tree into its build (harness-site repo,
      # scripts/fetch-docs.py) to serve the docs at /docs/. Ping the Amplify
      # webhook so that rebuild picks up this deploy. Optional: without the
      # AMPLIFY_DOCS_WEBHOOK_URL secret the step is skipped and only the
      # Pages site updates.
      - name: Trigger Amplify rebuild of harness.boldblack.ai
        if: env.AMPLIFY_DOCS_WEBHOOK_URL != ''
        run: |
          curl -sS -X POST --max-time 30 \
            -H "Content-Type: application/json" \
            -d '{"referrer":"harness-docs-deploy","source":"github-actions"}' \
            -o /dev/null -w "amplify webhook HTTP %{http_code}\n" \
            "$AMPLIFY_DOCS_WEBHOOK_URL"
